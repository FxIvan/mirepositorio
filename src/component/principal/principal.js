import React, { useContext } from 'react'
import GetScreen from '../getScreen/GetScreen'
import { Mobile } from '../mobile/Mobile'

export const Principal = () => {
  return (
    <div>
      <GetScreen/>
    </div>
  )
}
